# Changelog

## [Unreleased]

### Added
- **Restructuring**: Repository reorganized into `gpu_version`, `cpu_version`, and `comparison` directories.
- **wrappers**: Added `run_gpu.bat`/`py` and `run_cpu.bat`/`py` to enforce device usage without modifying original code.
- **configs**: Added `run_config.yml` for reproducible hyperparameters.
- **comparison**: Added `compare.py` and `run_all.bat` to automate performance benchmarking.
- **documentation**: Added READMEs for each section.
